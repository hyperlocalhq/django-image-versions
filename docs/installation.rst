============
Installation
============

At the command line::

    $ easy_install django-image-versions

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-image-versions
    $ pip install django-image-versions
